<%= form_for(@rsvp_status) do |f| %>
  <% if @rsvp_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsvp_status.errors.count, "error") %> prohibited this rsvp_status from being saved:</h2>

      <ul>
      <% @rsvp_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rsvp_id %><br>
    <%= f.text_field :rsvp_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
